specVersion: "v1beta"
serviceId: "Geoprofile"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "Geoprofile-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
